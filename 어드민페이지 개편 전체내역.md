파일명: ADMIN_CONSOLE_OVERHAUL_MASTER_SPEC.md
아래 내용을 복사해서 개발자에게 전달해 주세요.

🏗️ Admin Console Master Overhaul Specification
Project: ADC-GenAI Platform Admin Console (Phase 2) Date: 2026-01-19 Priority: Critical Objective: Transform the current mockup UI into a fully functional "Control Tower" for Data Operations, Simulation Management, and AI Tuning.

📌 Executive Summary (개요)
현재 어드민 콘솔의 단순 뷰(View) 기능을 넘어, 데이터 수집-검수-발행(Publication)의 전체 파이프라인을 통제하고, AI 에이전트의 로직을 세부적으로 튜닝할 수 있는 기능을 구현해야 합니다. 또한, 사용자 및 관리자를 위한 히스토리 아카이빙 기능이 추가됩니다.

1. 🔄 Data Operations (데이터 파이프라인)
데이터 관리 프로세스를 **[Source(수집) -> Staging(검수) -> Golden Set(확정)]**의 3단계로 명확히 분리합니다.

1.1. Data Sources (크롤링 제어)
Target: ClinicalTrials.gov, PubMed, Perplexity, Patent DB

New UI Features:

Scheduler Modal: 각 소스별 ⚙️ Settings 버튼 추가. Cron 포맷(예: 매일 03:00)으로 수집 주기 설정 기능.

Manual Trigger: Run Now 버튼 클릭 시 즉시 크롤링 워커 실행 (백엔드 /api/crawl/{source} 호출).

Live Logs: "Last Run: Success (15 new records)" 형태의 상태 로그 표시. 실패 시 에러 로그 및 재시도 버튼 활성화.

1.2. Staging Area (신규 탭: 데이터 검수소)
크롤링된 Raw Data가 골든셋으로 넘어가기 전 대기하는 장소입니다.

Tab Name: Inbox (Drafts)

UI Features:

Draft Table: AI가 1차 요약한 데이터 리스트 표시 (Source, Confidence Score 포함).

Comparison View: 화면 분할(Split View)로 좌측엔 '원본 텍스트', 우측엔 'AI 추출값'을 보여주어 검증 용이성 확보.

Review Actions:

✅ Approve: 승인 시 Golden Set 및 Vector DB로 이동.

✏️ Edit: AI 추출값이 틀린 경우 관리자가 수정 후 승인.

❌ Reject: 데이터 삭제.

1.3. Golden Set Editor (정형 데이터 관리)
확정된 ADC 약물 데이터를 관리하는 메인 DB 화면입니다.

New Columns & Features:

Chemical Structure: SMILES 코드를 **2D 구조 이미지(Canvas)**로 렌더링하여 표시 (RDKit/SmilesDrawer 활용). 클릭 시 확대 모달.

IP Status (Traffic Light):

🔴 Active (특허 보호 중)

🟡 Expiring Soon (만료 임박)

🟢 Expired (자유 사용 가능)

Failure Analysis: Outcome 컬럼 추가 (Success, Failure, Terminated). 실패 데이터는 붉은 배경색으로 구분하고 Failure Reason 필드 필수 노출.

Vector Sync Status: 데이터 수정 시 Re-index Needed 상태 표시 (Brain Icon 활용).

Manual Entry: + Add New Data 버튼으로 수동 데이터 입력 폼 제공.

1.4. Knowledge Base Manager (비정형/뉴스 관리)
정형 데이터(Golden Set)와 별도로, 뉴스나 논문 텍스트를 관리하는 탭입니다.

Daily Curation UI (Morning Briefing):

Evidence Card: 각 뉴스 아이템에 AI Relevance Score(점수), Source Tier(신뢰도 등급), Key Facts(3줄 요약) 표시.

Smart Action: Auto-Approve High Tier (신뢰도 높은 소스 일괄 승인) 버튼 추가.

Reasoning Tooltip: AI가 이 뉴스를 추천한 이유("경쟁사 임상 실패 관련 중요 뉴스임")를 툴팁으로 제공.

2. 🗂️ Project History & Simulation Logs
2.1. User Side: My Projects (내 보관함)
Location: 사용자 대시보드 사이드바.

Features:

Project List: 프로젝트명, 타겟, 날짜, 상태(Completed/Failed/Processing) 표시.

Click Action: 리스트 클릭 시 **결과 대시보드(Result Viewer)**로 이동.

Assets: PDF 리포트 다운로드 버튼 제공.

Real-time: 진행 중 상태 자동 업데이트 (Supabase Realtime).

2.2. Admin Side: Simulation Logs (관리자 로그)
Location: User Operations 내 하위 메뉴.

Features:

Full Monitoring: 모든 사용자의 시뮬레이션 요청 기록 조회.

Error Detail: 실패 건 클릭 시 상세 에러 로그(Stack Trace) 확인.

Admin Retry: 사용자를 대신하여 관리자 권한으로 재실행(Retry) 기능 (크레딧 미차감).

3. 🧠 AI Tuning (프롬프트 엔지니어링)
AI 시스템의 3가지 계층을 각각 독립적으로 제어할 수 있도록 탭을 재구성합니다.

Tab 1: Orchestrator (Main Router)
Role: 사용자 의도를 파악하고 적절한 에이전트를 호출하는 메인 두뇌.

Editable: 라우팅 로직 프롬프트 (예: "안구 독성 질문은 Toxicology Agent로 보내라").

Tab 2: Specialized Agents (Workers)
UI: 드롭다운으로 개별 에이전트 선택 (Structure, Toxicology, IP/Patent, Clinical).

Editable: 각 에이전트의 페르소나, 출력 포맷, 사고 과정(CoT) 프롬프트.

Tab 3: RAG Generator (Writer)
Role: 검색된 문서를 바탕으로 최종 답변을 작성하는 모듈.

Editable: 답변 스타일, 인용구(Citation) 형식, 비전문가용 용어 순화 지침 등.

4. 💾 Database Schema Changes (Backend)
위 기능 구현을 위해 Supabase 테이블에 다음 컬럼과 테이블을 추가해야 합니다.

SQL
-- 1. Golden Set Library 업데이트
ALTER TABLE golden_set_library 
ADD COLUMN outcome_type text CHECK (outcome_type IN ('Success', 'Failure', 'Terminated')),
ADD COLUMN failure_reason text,
ADD COLUMN ip_status text DEFAULT 'Unknown', -- Active, Expired, etc.
ADD COLUMN smiles_code text, -- 화학 구조식
ADD COLUMN patent_id text,
ADD COLUMN patent_expiry date;

-- 2. Knowledge Base (신규 테이블)
CREATE TABLE knowledge_base (
    id uuid DEFAULT gen_random_uuid() PRIMARY KEY,
    source_type text, -- 'News', 'PubMed', 'Report'
    title text,
    summary text,
    content text, -- 원문
    relevance_score float, -- AI 중요도 점수
    source_tier int, -- 1(High) ~ 3(Low)
    ai_reasoning text, -- 추천 사유
    rag_status text DEFAULT 'pending', -- pending, indexed, excluded
    created_at timestamptz DEFAULT now()
);

-- 3. Simulation Logs (Projects 테이블 활용)
-- 기존 Projects 테이블에 error_log 컬럼이 있는지 확인하고 없으면 추가
ALTER TABLE projects ADD COLUMN error_detail text;
5. 🛠️ Development Checklist (Check before Push)
[ ] Data Ops: 크롤링 스케줄러가 백엔드 Cron과 연동되는가?

[ ] Staging: Inbox에서 '승인' 누르면 Golden Set으로 이동하는가?

[ ] Chemistry: SMILES 코드가 캔버스에 화학 구조로 그려지는가?

[ ] Failure Logic: 실패한 약물 데이터도 조회 및 필터링이 가능한가?

[ ] History: 사용자가 지난 프로젝트 결과를 다시 열어볼 수 있는가?

[ ] Knowledge Base: 뉴스 데이터를 '승인'하면 RAG 인덱스에 포함되는가?

[ ] AI Tuning: 오케스트레이터와 RAG 답변 프롬프트가 분리되어 저장되는가?