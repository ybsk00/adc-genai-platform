/**
 * Design Runs Page Wrapper
 * Wraps the DesignRunsTab component for the admin route
 */
import { DesignRunsTab } from '@/components/admin/data/DesignRunsTab';

export function DesignRunsPage() {
    return <DesignRunsTab />;
}
